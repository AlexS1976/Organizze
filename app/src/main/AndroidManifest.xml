<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.organizze1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Organizze">
        <activity android:name=".Activitys.SaidaActivity" android:theme="@style/despesaTheme"></activity>
        <activity android:name=".Activitys.EntradaActivity" android:theme="@style/receitaTheme" />
        <activity
            android:name=".Activitys.PrincipalActivity"
            android:label="@string/title_activity_principal"
            android:theme="@style/Theme.Organizze.NoActionBar" />
        <activity android:name=".Activitys.CadastrarActivity" />
        <activity android:name=".Activitys.LoginActivity" />
        <activity
            android:name=".Activitys.MainActivity"
            android:theme="@style/Theme.Intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>